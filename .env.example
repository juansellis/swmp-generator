NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
# Server-only: Places API (v1) for address validation, Geocoding and Distance Matrix
GOOGLE_MAPS_API_KEY=
# Client: Places Autocomplete (restrict to your domain in Google Cloud Console)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
MOCK_SWMP=1

# Optional: base URL for PDF export (used when generating report PDF server-side). Defaults to VERCEL_URL on Vercel.
# NEXT_PUBLIC_APP_URL=https://your-domain.com
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
